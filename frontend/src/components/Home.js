export function Home () {
  return (
    <div className="Home">
        <h1>Home</h1>
        <p>This is a simple app project that gets data from an express server and displays in a table on the stock page.</p>
        <p>The app is built with React, React Router, and Bootstrap.</p>
        <p>The express server sends over static JSON data.</p>
        <p>This project was worked on by Martins Musinskis and Ben Spelman</p>
    </div>
  );
}